# Dev Environment

This folder is reserved for the shared platform Terraform composition for the `dev` environment.

Use it for development-stage platform infrastructure that supports safe iteration on the platform foundation itself.

Keep product-specific development infrastructure in separate data product repositories.
