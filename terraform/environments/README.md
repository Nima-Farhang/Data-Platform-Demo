# Environments

This folder contains environment-specific Terraform compositions for the shared platform.

Each environment should assemble reusable platform modules with the inputs, naming, and controls required for that stage.

These environment definitions are for platform infrastructure only, not for individual data products.
