# Test Environment

This folder is reserved for the shared platform Terraform composition for the `test` environment.

Use it for validating platform changes in a controlled pre-production stage.

Only shared platform infrastructure belongs here.
