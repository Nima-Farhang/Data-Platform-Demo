# Prod Environment

This folder is reserved for the shared platform Terraform composition for the `prod` environment.

Use it for production-grade platform infrastructure that will be shared across data products.

Product-specific production infrastructure belongs in separate repositories owned by those products.
