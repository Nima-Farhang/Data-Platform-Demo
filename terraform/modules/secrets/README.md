# Secrets Module

This module creates the V1 Secrets Manager baseline for Data Platform Demo.

It includes only:

- One placeholder AWS Secrets Manager secret

The placeholder value is not a real credential. Real credentials must be created or rotated outside source control.
